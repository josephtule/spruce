pub struct OrbitalElements {
    pub sma: f64,
    pub ecc: f64,
    pub inc: f64,
    pub raan: f64, // Right ascension of the ascending node
    pub aop: f64,
    pub ta: f64,
    pub param: f64,
}
